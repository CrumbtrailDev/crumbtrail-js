# crumbtrail-core

Browser SDK for Crumbtrail session capture.

Use this package in browser applications that need to capture user interactions,
network context, and runtime evidence for a local Crumbtrail server.

```bash
npm i crumbtrail-core
```

For the local server, CLI, and MCP evidence tools, install `crumbtrail-node`.
